{% extends 'base.html.twig' %}
{% block body %}

    <div class="row justify-content-center">
        <div class="col-10">
            <div class="row justify-content-center">
                <div class="col-2">
                    <a href="{{ path('gameGuestBoard') }}" class="btn btn-success btn-block mt-5">Play</a>
                </div>
            </div>
        </div>
    </div>

{% endblock body %}